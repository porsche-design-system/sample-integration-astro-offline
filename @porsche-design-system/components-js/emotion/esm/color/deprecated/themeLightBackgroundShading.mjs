import { colorBackdropLight } from '../../tokens/dist/esm/color/light/background/colorBackdropLight.mjs';

/** @deprecated since v4.0.0, will be removed with next major release. Use colorBackdropLight instead. */
const themeLightBackgroundShading = colorBackdropLight;

export { themeLightBackgroundShading };
