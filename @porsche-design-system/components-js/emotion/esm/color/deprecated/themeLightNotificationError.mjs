import { colorErrorLight } from '../../tokens/dist/esm/color/light/semantic/colorErrorLight.mjs';

/** @deprecated since v4.0.0, will be removed with next major release. Use colorErrorLight instead. */
const themeLightNotificationError = colorErrorLight;

export { themeLightNotificationError };
