import { colorCanvas as colorCanvas$1 } from '../../tokens/dist/esm/color/light-dark/background/colorCanvas.mjs';

const colorCanvas = `var(--_color-canvas, ${colorCanvas$1})`;

export { colorCanvas };
