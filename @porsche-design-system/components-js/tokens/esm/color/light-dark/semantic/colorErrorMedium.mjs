import { colorErrorMediumDark } from '../../dark/semantic/colorErrorMediumDark.mjs';
import { colorErrorMediumLight } from '../../light/semantic/colorErrorMediumLight.mjs';

/** Holds the **error-medium** color, typically used for text or border. */
const colorErrorMedium = `light-dark(${colorErrorMediumLight},${colorErrorMediumDark})`;

export { colorErrorMedium };
