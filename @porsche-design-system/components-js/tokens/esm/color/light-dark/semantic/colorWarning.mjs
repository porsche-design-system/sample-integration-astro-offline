import { colorWarningDark } from '../../dark/semantic/colorWarningDark.mjs';
import { colorWarningLight } from '../../light/semantic/colorWarningLight.mjs';

/** Holds the **warning** color, typically used for text. */
const colorWarning = `light-dark(${colorWarningLight},${colorWarningDark})`;

export { colorWarning };
