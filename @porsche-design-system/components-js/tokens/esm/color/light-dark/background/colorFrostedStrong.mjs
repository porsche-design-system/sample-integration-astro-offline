import { colorFrostedStrongDark } from '../../dark/background/colorFrostedStrongDark.mjs';
import { colorFrostedStrongLight } from '../../light/background/colorFrostedStrongLight.mjs';

/** Holds the **frosted-strong** color, typically used as a stronger frosted background. */
const colorFrostedStrong = `light-dark(${colorFrostedStrongLight},${colorFrostedStrongDark})`;

export { colorFrostedStrong };
