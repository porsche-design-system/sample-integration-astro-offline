import { palette } from '../../palette.mjs';

/** Holds the **frosted-soft** color (light), typically used as a background `:hover`. */
const colorFrostedSoftLight = palette.light.grey['50a'];

export { colorFrostedSoftLight };
