import { palette } from '../../palette.mjs';

/** Holds the **frosted** color (light), typically used as a background in combination with a blur effect. */
const colorFrostedLight = palette.light.grey['100a'];

export { colorFrostedLight };
