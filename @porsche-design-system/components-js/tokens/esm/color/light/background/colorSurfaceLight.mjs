import { palette } from '../../palette.mjs';

/** Holds the **surface** color (light), typically used for surfaces. */
const colorSurfaceLight = palette.light.grey['50'];

export { colorSurfaceLight };
