import { palette } from '../../palette.mjs';

/** Holds the **backdrop** color, typically used for backdrops. */
const colorBackdropLight = palette.light.grey['500a'];

export { colorBackdropLight };
