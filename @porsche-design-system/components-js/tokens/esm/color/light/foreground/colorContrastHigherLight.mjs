import { palette } from '../../palette.mjs';

/** Holds the **contrast-higher** color (light), typically used for text. */
const colorContrastHigherLight = palette.light.grey['800a'];

export { colorContrastHigherLight };
