import { palette } from '../../palette.mjs';

/** Holds the **contrast-medium** color (light), typically used for text. */
const colorContrastMediumLight = palette.light.grey['600a'];

export { colorContrastMediumLight };
