import { palette } from '../../palette.mjs';

/** Holds the **contrast-high** color (light), typically used for text. */
const colorContrastHighLight = palette.light.grey['700a'];

export { colorContrastHighLight };
