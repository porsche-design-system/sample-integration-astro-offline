import { palette } from '../../palette.mjs';

/** Holds the **success** color (light), typically used for text. */
const colorSuccessLight = palette.light.green['950'];

export { colorSuccessLight };
