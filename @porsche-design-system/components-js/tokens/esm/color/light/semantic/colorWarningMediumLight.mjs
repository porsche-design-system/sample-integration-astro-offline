import { palette } from '../../palette.mjs';

/** Holds the **warning-medium** color (light), typically used for text or border. */
const colorWarningMediumLight = palette.light.yellow['600a'];

export { colorWarningMediumLight };
