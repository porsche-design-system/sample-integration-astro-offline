import { palette } from '../../palette.mjs';

/** Holds the **success-low** color (light), typically used for text or border. */
const colorSuccessLowLight = palette.light.green['100a'];

export { colorSuccessLowLight };
