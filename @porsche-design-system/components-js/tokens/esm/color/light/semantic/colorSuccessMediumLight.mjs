import { palette } from '../../palette.mjs';

/** Holds the **success-medium** color (light), typically used for text or border. */
const colorSuccessMediumLight = palette.light.green['600a'];

export { colorSuccessMediumLight };
