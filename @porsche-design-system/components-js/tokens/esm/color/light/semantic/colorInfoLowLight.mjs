import { palette } from '../../palette.mjs';

/** Holds the **info-low** color (light), typically used for text or border. */
const colorInfoLowLight = palette.light.blue['100a'];

export { colorInfoLowLight };
