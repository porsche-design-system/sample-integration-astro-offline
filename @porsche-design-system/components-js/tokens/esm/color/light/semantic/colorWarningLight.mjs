import { palette } from '../../palette.mjs';

/** Holds the **warning** color (light), typically used for text. */
const colorWarningLight = palette.light.yellow['950'];

export { colorWarningLight };
