import { palette } from '../../palette.mjs';

/** Holds the **error-low** color (light), typically used for text or border. */
const colorErrorLowLight = palette.light.red['100a'];

export { colorErrorLowLight };
