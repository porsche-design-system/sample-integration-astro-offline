import { palette } from '../../palette.mjs';

/** Holds the **warning-low** color (light), typically used for text or border. */
const colorWarningLowLight = palette.light.yellow['100a'];

export { colorWarningLowLight };
