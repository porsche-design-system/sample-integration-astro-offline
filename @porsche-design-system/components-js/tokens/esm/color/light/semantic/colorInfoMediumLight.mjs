import { palette } from '../../palette.mjs';

/** Holds the **info-medium** color (light), typically used for text or border. */
const colorInfoMediumLight = palette.light.blue['600a'];

export { colorInfoMediumLight };
