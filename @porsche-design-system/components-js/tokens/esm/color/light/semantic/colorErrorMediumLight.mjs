import { palette } from '../../palette.mjs';

/** Holds the **error-medium** color (light), typically used for text or border. */
const colorErrorMediumLight = palette.light.red['600a'];

export { colorErrorMediumLight };
