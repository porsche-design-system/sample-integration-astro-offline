import { palette } from '../../palette.mjs';

/** Holds the **frosted-strong** color (dark), typically used as a stronger frosted background. */
const colorFrostedStrongDark = palette.dark.grey['300a'];

export { colorFrostedStrongDark };
