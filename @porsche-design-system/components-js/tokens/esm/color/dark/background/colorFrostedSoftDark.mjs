import { palette } from '../../palette.mjs';

/** Holds the **frosted-soft** color (dark), typically used as a background `:hover`. */
const colorFrostedSoftDark = palette.dark.grey['100a'];

export { colorFrostedSoftDark };
