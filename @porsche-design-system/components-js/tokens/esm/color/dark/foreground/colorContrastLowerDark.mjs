import { palette } from '../../palette.mjs';

/** Holds the **contrast-lower** color (dark), intended only for decorative elements, as it is not accessibility-compliant. */
const colorContrastLowerDark = palette.dark.grey['300a'];

export { colorContrastLowerDark };
