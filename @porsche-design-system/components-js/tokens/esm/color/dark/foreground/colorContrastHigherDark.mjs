import { palette } from '../../palette.mjs';

/** Holds the **contrast-higher** color (dark), typically used for text. */
const colorContrastHigherDark = palette.dark.grey['800a'];

export { colorContrastHigherDark };
