import { palette } from '../../palette.mjs';

/** Holds the **contrast-high** color (dark), typically used for text. */
const colorContrastHighDark = palette.dark.grey['700a'];

export { colorContrastHighDark };
