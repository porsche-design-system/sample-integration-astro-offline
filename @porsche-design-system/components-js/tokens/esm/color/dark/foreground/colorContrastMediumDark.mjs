import { palette } from '../../palette.mjs';

/** Holds the **contrast-medium** color (dark), typically used for text. */
const colorContrastMediumDark = palette.dark.grey['600a'];

export { colorContrastMediumDark };
