import { palette } from '../../palette.mjs';

/** Holds the **info-medium** color (dark), typically used for text or border. */
const colorInfoMediumDark = palette.dark.blue['600a'];

export { colorInfoMediumDark };
