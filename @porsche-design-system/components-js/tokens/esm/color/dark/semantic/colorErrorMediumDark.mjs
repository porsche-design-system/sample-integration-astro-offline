import { palette } from '../../palette.mjs';

/** Holds the **error-medium** color (dark), typically used for text or border. */
const colorErrorMediumDark = palette.dark.red['600a'];

export { colorErrorMediumDark };
