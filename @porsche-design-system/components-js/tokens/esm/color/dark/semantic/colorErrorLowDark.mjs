import { palette } from '../../palette.mjs';

/** Holds the **error-low** color (dark), typically used for text or border. */
const colorErrorLowDark = palette.dark.red['100a'];

export { colorErrorLowDark };
