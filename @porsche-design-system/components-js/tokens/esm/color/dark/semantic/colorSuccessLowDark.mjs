import { palette } from '../../palette.mjs';

/** Holds the **success-low** color (dark), typically used for text or border. */
const colorSuccessLowDark = palette.dark.green['100a'];

export { colorSuccessLowDark };
