import { palette } from '../../palette.mjs';

/** Holds the **warning-medium** color (dark), typically used for text or border. */
const colorWarningMediumDark = palette.dark.yellow['600a'];

export { colorWarningMediumDark };
