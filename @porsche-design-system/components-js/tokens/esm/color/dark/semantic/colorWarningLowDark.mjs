import { palette } from '../../palette.mjs';

/** Holds the **warning-low** color (dark), typically used for text or border. */
const colorWarningLowDark = palette.dark.yellow['100a'];

export { colorWarningLowDark };
