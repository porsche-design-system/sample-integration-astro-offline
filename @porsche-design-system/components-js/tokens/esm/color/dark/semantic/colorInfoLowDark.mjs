import { palette } from '../../palette.mjs';

/** Holds the **info-low** color (dark), typically used for text or border. */
const colorInfoLowDark = palette.dark.blue['100a'];

export { colorInfoLowDark };
