import { palette } from '../../palette.mjs';

/** Holds the **success-medium** color (dark), typically used for text or border. */
const colorSuccessMediumDark = palette.dark.green['600a'];

export { colorSuccessMediumDark };
