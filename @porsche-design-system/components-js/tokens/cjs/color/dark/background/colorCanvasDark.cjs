'use strict';

var palette = require('../../palette.cjs');

/** Holds the **canvas** color (dark), typically used for surfaces. */
const colorCanvasDark = palette.palette.dark.grey['50'];

exports.colorCanvasDark = colorCanvasDark;
