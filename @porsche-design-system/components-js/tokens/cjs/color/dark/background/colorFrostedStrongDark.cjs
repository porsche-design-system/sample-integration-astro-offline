'use strict';

var palette = require('../../palette.cjs');

/** Holds the **frosted-strong** color (dark), typically used as a stronger frosted background. */
const colorFrostedStrongDark = palette.palette.dark.grey['300a'];

exports.colorFrostedStrongDark = colorFrostedStrongDark;
