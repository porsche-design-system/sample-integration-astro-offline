'use strict';

var palette = require('../../palette.cjs');

/** Holds the **frosted** color (dark), typically used as a background in combination with a blur effect. */
const colorFrostedDark = palette.palette.dark.grey['200a'];

exports.colorFrostedDark = colorFrostedDark;
