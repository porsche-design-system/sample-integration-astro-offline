'use strict';

var palette = require('../../palette.cjs');

/** Holds the **contrast-lower** color (dark), intended only for decorative elements, as it is not accessibility-compliant. */
const colorContrastLowerDark = palette.palette.dark.grey['300a'];

exports.colorContrastLowerDark = colorContrastLowerDark;
