'use strict';

var palette = require('../../palette.cjs');

/** Holds the **contrast-higher** color (dark), typically used for text. */
const colorContrastHigherDark = palette.palette.dark.grey['800a'];

exports.colorContrastHigherDark = colorContrastHigherDark;
