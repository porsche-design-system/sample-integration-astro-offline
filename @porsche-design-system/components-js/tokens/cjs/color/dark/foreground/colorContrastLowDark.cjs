'use strict';

var palette = require('../../palette.cjs');

/** Holds the **contrast-low** color (dark), intended only for decorative elements, as it is not accessibility-compliant. */
const colorContrastLowDark = palette.palette.dark.grey['500a'];

exports.colorContrastLowDark = colorContrastLowDark;
