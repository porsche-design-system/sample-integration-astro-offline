'use strict';

var palette = require('../../palette.cjs');

/** Holds the **warning-medium** color (dark), typically used for text or border. */
const colorWarningMediumDark = palette.palette.dark.yellow['600a'];

exports.colorWarningMediumDark = colorWarningMediumDark;
