'use strict';

var palette = require('../../palette.cjs');

/** Holds the **error-low** color (dark), typically used for text or border. */
const colorErrorLowDark = palette.palette.dark.red['100a'];

exports.colorErrorLowDark = colorErrorLowDark;
