'use strict';

var palette = require('../../palette.cjs');

/** Holds the **info** color (dark), typically used for text. */
const colorInfoDark = palette.palette.dark.blue['950'];

exports.colorInfoDark = colorInfoDark;
