'use strict';

var palette = require('../../palette.cjs');

/** Holds the **warning** color (dark), typically used for text. */
const colorWarningDark = palette.palette.dark.yellow['950'];

exports.colorWarningDark = colorWarningDark;
