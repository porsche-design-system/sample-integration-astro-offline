'use strict';

var palette = require('../../palette.cjs');

/** Holds the **error-medium** color (dark), typically used for text or border. */
const colorErrorMediumDark = palette.palette.dark.red['600a'];

exports.colorErrorMediumDark = colorErrorMediumDark;
