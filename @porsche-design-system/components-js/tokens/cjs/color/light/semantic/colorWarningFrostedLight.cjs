'use strict';

/** Holds the **warning-frosted** color (light), typically used as background in combination with a blur effect. */
const colorWarningFrostedLight = 'hsl(40 100% 90% / 0.55)';

exports.colorWarningFrostedLight = colorWarningFrostedLight;
