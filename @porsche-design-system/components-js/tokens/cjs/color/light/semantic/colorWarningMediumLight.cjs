'use strict';

var palette = require('../../palette.cjs');

/** Holds the **warning-medium** color (light), typically used for text or border. */
const colorWarningMediumLight = palette.palette.light.yellow['600a'];

exports.colorWarningMediumLight = colorWarningMediumLight;
