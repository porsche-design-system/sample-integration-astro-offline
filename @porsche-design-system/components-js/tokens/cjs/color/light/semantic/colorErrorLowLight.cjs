'use strict';

var palette = require('../../palette.cjs');

/** Holds the **error-low** color (light), typically used for text or border. */
const colorErrorLowLight = palette.palette.light.red['100a'];

exports.colorErrorLowLight = colorErrorLowLight;
