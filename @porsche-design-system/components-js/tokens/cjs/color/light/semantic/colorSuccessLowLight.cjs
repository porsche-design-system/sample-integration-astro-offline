'use strict';

var palette = require('../../palette.cjs');

/** Holds the **success-low** color (light), typically used for text or border. */
const colorSuccessLowLight = palette.palette.light.green['100a'];

exports.colorSuccessLowLight = colorSuccessLowLight;
