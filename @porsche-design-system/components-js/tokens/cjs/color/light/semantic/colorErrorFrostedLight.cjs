'use strict';

/** Holds the **error-frosted** color (light), typically used as background in combination with a blur effect. */
const colorErrorFrostedLight = 'hsl(0 100% 90% / 0.55)';

exports.colorErrorFrostedLight = colorErrorFrostedLight;
