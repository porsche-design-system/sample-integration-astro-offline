'use strict';

var palette = require('../../palette.cjs');

/** Holds the **info-low** color (light), typically used for text or border. */
const colorInfoLowLight = palette.palette.light.blue['100a'];

exports.colorInfoLowLight = colorInfoLowLight;
