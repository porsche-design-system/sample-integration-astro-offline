'use strict';

var palette = require('../../palette.cjs');

/** Holds the **warning-low** color (light), typically used for text or border. */
const colorWarningLowLight = palette.palette.light.yellow['100a'];

exports.colorWarningLowLight = colorWarningLowLight;
