'use strict';

var palette = require('../../palette.cjs');

/** Holds the **warning** color (light), typically used for text. */
const colorWarningLight = palette.palette.light.yellow['950'];

exports.colorWarningLight = colorWarningLight;
