'use strict';

var palette = require('../../palette.cjs');

/** Holds the **error-medium** color (light), typically used for text or border. */
const colorErrorMediumLight = palette.palette.light.red['600a'];

exports.colorErrorMediumLight = colorErrorMediumLight;
