'use strict';

var palette = require('../../palette.cjs');

/** Holds the **info** color (light), typically used for text. */
const colorInfoLight = palette.palette.light.blue['950'];

exports.colorInfoLight = colorInfoLight;
