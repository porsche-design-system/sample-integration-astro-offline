'use strict';

var palette = require('../../palette.cjs');

/** Holds the **error** color (light), typically used for text. */
const colorErrorLight = palette.palette.light.red['950'];

exports.colorErrorLight = colorErrorLight;
