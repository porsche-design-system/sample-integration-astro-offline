'use strict';

var palette = require('../../palette.cjs');

/** Holds the **info-medium** color (light), typically used for text or border. */
const colorInfoMediumLight = palette.palette.light.blue['600a'];

exports.colorInfoMediumLight = colorInfoMediumLight;
