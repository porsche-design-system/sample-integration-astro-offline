'use strict';

var palette = require('../../palette.cjs');

/** Holds the **contrast-low** color (light), intended only for decorative elements, as it is not accessibility-compliant. */
const colorContrastLowLight = palette.palette.light.grey['500a'];

exports.colorContrastLowLight = colorContrastLowLight;
