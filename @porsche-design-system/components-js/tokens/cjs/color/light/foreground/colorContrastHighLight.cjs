'use strict';

var palette = require('../../palette.cjs');

/** Holds the **contrast-high** color (light), typically used for text. */
const colorContrastHighLight = palette.palette.light.grey['700a'];

exports.colorContrastHighLight = colorContrastHighLight;
