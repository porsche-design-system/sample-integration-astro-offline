'use strict';

var palette = require('../../palette.cjs');

/** Holds the **contrast-higher** color (light), typically used for text. */
const colorContrastHigherLight = palette.palette.light.grey['800a'];

exports.colorContrastHigherLight = colorContrastHigherLight;
