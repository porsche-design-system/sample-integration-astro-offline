'use strict';

var palette = require('../../palette.cjs');

/** Holds the **contrast-lower** color (light), intended only for decorative elements, as it is not accessibility-compliant. */
const colorContrastLowerLight = palette.palette.light.grey['300a'];

exports.colorContrastLowerLight = colorContrastLowerLight;
