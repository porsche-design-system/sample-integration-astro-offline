'use strict';

var palette = require('../../palette.cjs');

/** Holds the **frosted-strong** color (light), typically used as a stronger frosted background. */
const colorFrostedStrongLight = palette.palette.light.grey['200a'];

exports.colorFrostedStrongLight = colorFrostedStrongLight;
