'use strict';

var palette = require('../../palette.cjs');

/** Holds the **frosted-soft** color (light), typically used as a background `:hover`. */
const colorFrostedSoftLight = palette.palette.light.grey['50a'];

exports.colorFrostedSoftLight = colorFrostedSoftLight;
