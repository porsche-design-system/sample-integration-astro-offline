'use strict';

var palette = require('../../palette.cjs');

/** Holds the **frosted** color (light), typically used as a background in combination with a blur effect. */
const colorFrostedLight = palette.palette.light.grey['100a'];

exports.colorFrostedLight = colorFrostedLight;
