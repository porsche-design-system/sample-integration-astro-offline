'use strict';

var palette = require('../../palette.cjs');

/** Holds the **surface** color (light), typically used for surfaces. */
const colorSurfaceLight = palette.palette.light.grey['50'];

exports.colorSurfaceLight = colorSurfaceLight;
