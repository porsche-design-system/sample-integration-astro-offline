import { colorBackdrop as colorBackdrop$1 } from '../../tokens/dist/esm/color/light-dark/background/colorBackdrop.mjs';

const colorBackdrop = `var(--_color-backdrop, ${colorBackdrop$1})`;

export { colorBackdrop };
